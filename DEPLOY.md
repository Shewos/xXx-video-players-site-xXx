# Деплой на GitHub Pages за 3 минуты

## Шаг 1: Создайте репозиторий

1. Откройте https://github.com/new
2. Название: `video-players-site`
3. **Public** ✅ (для бесплатного Pages)
4. Нажмите **Create repository**

---

## Шаг 2: Загрузите index.html

**Вариант А: Через браузер (проще)**

1. На странице репозитория → **Add file** → **Upload files**
2. Перетащите файл `index.html` из папки `video-players-site`
3. **Commit changes**

**Вариант Б: Через Git**

```bash
cd C:\Users\FENRIR\Desktop\video-players-site
git init
git add index.html README.md
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/video-players-site.git
git push -u origin main
```

---

## Шаг 3: Включите GitHub Pages

1. В репозитории → **Settings** (вверху справа)
2. Слева в меню → **Pages**
3. **Source**: Deploy from a branch
4. **Branch**: main → **/ (root)**
5. **Save**

⏳ Подождите 1-2 минуты

---

## Шаг 4: Получите ссылку

Ваш сайт: `https://ваш-username.github.io/video-players-site/`

Проверьте: `https://ваш-username.github.io/video-players-site/?kp=195332`

---

## Шаг 5: Обновите бота

В файле `bot.py` найдите функцию `cmd_players`:

```python
# Замените эту строку:
base_url = "https://your-username.github.io/video-players"

# На вашу реальную:
base_url = "https://ваш-username.github.io/video-players-site"
```

---

## Шаг 6: Зарегистрируйте домен в Kodik (опционально)

1. https://kodik.biz/cabinet/registration
2. Заполните форму → Регистрация
3. **Настройки** → **Домены**
4. Добавьте: `ваш-username.github.io`
5. **Сохранить**

То же самое для Alloha, HDRezka (если нужно).

---

## ✅ Готово!

Теперь:
- `/players` в боте → получаете ссылку с токеном
- Сайт работает с 11 плеерами
- Защита от спама токенами

---

## 🔐 Включить защиту токеном

В `index.html` найдите строки 99-107:

```javascript
// Защита от прямого доступа (раскомментируйте после деплоя)
// if (!token) {
//     document.body.innerHTML = `
//         <div style="text-align:center; margin-top:100px;">
//             <h1 style="color:red;">❌ Access Denied</h1>
//             <p>Получите токен через Telegram бота: /players</p>
//         </div>
//     `;
//     throw new Error('No token');
// }
```

**Раскомментируйте** (уберите `//`):

```javascript
if (!token) {
    document.body.innerHTML = `
        <div style="text-align:center; margin-top:100px;">
            <h1 style="color:red;">❌ Access Denied</h1>
            <p>Получите токен через Telegram бота: /players</p>
        </div>
    `;
    throw new Error('No token');
}
```

---

## ⚠️ Private репозиторий?

**GitHub Pro** = $4/месяц:
- Код скрыт от других пользователей
- НО сайт GitHub Pages всё равно публичный
- Защита токенами всё равно нужна

**Рекомендация:** используйте Free + токены (достаточно безопасно)

---

## 🆘 Проблемы?

**404 Not Found**
→ Подождите 5 минут, Pages разворачивается

**Плееры не работают**
→ Зарегистрируйте домен в Kodik/Alloha

**Access Denied**
→ Получите токен: `/players` в боте

**CORS ошибки**
→ Используйте `https://` (не `http://`)
