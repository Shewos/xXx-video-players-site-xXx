# 🔄 Работа с GitHub репозиторием из VS Code

## ✅ Да, можно редактировать код локально и синхронизировать с GitHub!

### Как это работает:

```
VS Code (локально) ←→ Git ←→ GitHub (облако)
      ↓
Редактируете код
      ↓
git add / commit / push
      ↓
GitHub Pages автоматически обновляется
```

---

## 📝 Пошаговая настройка

### Шаг 1: Клонируйте репозиторий после создания

```powershell
# После создания репозитория на GitHub:
cd C:\Users\FENRIR\Desktop
git clone https://github.com/ваш-username/video-players-site.git
cd video-players-site

# Скопируйте файлы из старой папки
copy ..\video-players-site-old\* .

# Закоммитьте
git add .
git commit -m "Initial commit"
git push
```

---

### Шаг 2: Редактируйте в VS Code

1. **File → Open Folder** → выберите `video-players-site`
2. Редактируйте `index.html`, `README.md` и т.д.
3. Сохраняйте изменения (Ctrl+S)

---

### Шаг 3: Синхронизируйте с GitHub

**Вариант А: Через VS Code UI (проще)**

1. Откройте Source Control (Ctrl+Shift+G)
2. Увидите изменённые файлы
3. Нажмите **+** (Stage All Changes)
4. Введите commit message: "Обновил плееры"
5. Нажмите **✓** (Commit)
6. Нажмите **⋯** → Push

**Вариант Б: Через терминал**

```powershell
git add .
git commit -m "Обновил плееры"
git push
```

---

### Шаг 4: GitHub Pages обновится автоматически

⏳ 1-2 минуты после push → сайт обновлён!

---

## 🔄 Рабочий процесс

```
1. Редактируете index.html в VS Code
2. Ctrl+S (сохранить)
3. Ctrl+Shift+G (Source Control)
4. Commit + Push
5. Через 1 минуту изменения на сайте
```

---

## 💡 Полезные команды

### Проверить статус:
```bash
git status
```

### Отменить изменения:
```bash
git checkout index.html
```

### Посмотреть историю:
```bash
git log --oneline
```

### Создать ветку для экспериментов:
```bash
git checkout -b test-feature
# Редактируйте
git push -u origin test-feature
```

---

## 🎯 Итого

**Да, полностью удобно!**

1. Код в VS Code (локально)
2. Git синхронизирует с GitHub
3. GitHub Pages автоматически деплоит
4. Сайт обновляется через 1-2 минуты

**Как сейчас работаете, так и продолжите** — просто добавится `git push` для публикации.
